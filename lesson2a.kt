fun main(){
    // if statements 
    // only works for true conditions
    // example1: check if number is positive
    val number = -55
    if (number > 0){
        println("the number is positive")
    }

    // example 2
    // check if one can vote 
    val age = 19
    if (age >= 18){
        println("you can vote")
    }

    // check if one can donate blood 
    val weight = 65
    if (weight >= 50){
        println("you can donate blood")
    }

    // check if gender is female
    val gender = "female"
    if (gender == "female"){
    println("you can attend the seminar")
    }

    // check if colour is red
    val colour= "red"
    if(colour=="red"){
        println("you can paint")
    }

    // check if country is Kenya 
    val capital_city = "Nairobi"
    if (capital_city == "Nairobi"){
        println("the country is Kenya")
    }
}